#include<iostream>
#include<algorithm>
#include<string>
#include<cstdio>
#include<string>
using namespace std;

int main(){

	string s;

	cin >> s;

	bool neg = s[0] == '-';

	if ( neg ){
		s = s.substr(1);
		if ( s <= "128" )
	}

	return 0;
}
